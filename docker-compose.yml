services:
  mcp:
    build:
      context: .
      dockerfile: Dockerfile.mcp
    environment:
      - DISPLAY=${DISPLAY}
      - MCP_PACKAGE=${MCP_PACKAGE}
      - MCP_CMD=${MCP_CMD}
      - MCP_ARGS=${MCP_ARGS:---host 0.0.0.0 --user-data-dir /data --allowed-hosts mcp:3333,localhost:3333,127.0.0.1:3333 --no-sandbox}
      - PORT=3333
      - HEADLESS=false
    ports:
      - "3333:3333"
    volumes:
      - mcp-data:/data
      - /tmp/.X11-unix:/tmp/.X11-unix:rw

  api:
    build:
      context: .
      dockerfile: Dockerfile
    env_file:
      - .env
    environment:
      - MCP_ENDPOINT=http://mcp:3333/mcp
      - MCP_USER_DATA_DIR=/data
      - OPENROUTER_API_KEY=${OPENROUTER_API_KEY}
      - OPENROUTER_MODEL=${OPENROUTER_MODEL}
    ports:
      - "8000:8000"
    depends_on:
      - mcp

volumes:
  mcp-data:
